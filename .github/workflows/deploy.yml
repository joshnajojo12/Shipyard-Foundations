name: Deploy Website

on:
push:
branches:
- main

permissions:
pages: write
id-token: write

jobs:
deploy:
runs-on: ubuntu-latest

```
steps:
  - name: Checkout
    uses: actions/checkout@v3

  - name: Setup Pages
    uses: actions/configure-pages@v3

  - name: Upload Files
    uses: actions/upload-pages-artifact@v2
    with:
      path: ./Pavithra-menona%40mulearn/Task-3

  - name: Deploy
    uses: actions/deploy-pages@v2
```
